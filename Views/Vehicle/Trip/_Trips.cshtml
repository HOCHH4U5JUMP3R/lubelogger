@using CarCareTracker.Helper
@inject IConfigHelper config
@inject ITranslationHelper translator
@model List<Note>
@{
    var userConfig = config.GetUserConfig(User);
    var userLanguage = userConfig.UserLanguage;
}
<div class="vehicleTabPane">
    <div class="d-flex justify-content-between my-2">
        <span class="badge bg-success">Fahrten: @Model.Count</span>
        <div>
            <input type="file" id="tripCsvUpload" accept=".csv" class="d-none" onchange="importTripsCsv(this)">
            <button class="btn btn-secondary" onclick="$('#tripCsvUpload').click()">CSV importieren</button>
            <button class="btn btn-primary" onclick="showAddTripModal()">Fahrt hinzufügen</button>
        </div>
    </div>
    <table class="table table-hover">
        <thead><tr><th>Beschreibung</th><th>Details</th><th>Anhänge</th></tr></thead>
        <tbody>
            @foreach (var trip in Model)
            {
                <tr style="cursor:pointer;" onclick="showEditTripModal(@trip.Id)">
                    <td>@trip.Description</td>
                    <td style="white-space:pre-line;">@trip.NoteText</td>
                    <td>@await Html.PartialAsync("_AttachmentColumn", trip.Files)</td>
                </tr>
            }
        </tbody>
    </table>
</div>
<div class="modal fade" data-bs-focus="false" id="tripModal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-lg"><div class="modal-content" id="tripModalContent"></div></div>
</div>
